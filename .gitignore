__pycache__/
*.pyc
.venv/
credenciais/
src/.claude/
.git-token

# dados/ é estado de execução (roteiros, fila, histórico, mídia
# gerada) — recriado automaticamente pelos scripts, não é código.
dados/
